# Scripts 归档

本目录用于存放**不再被当前运行链路引用**、但仍有历史价值/可能复用价值的脚本与片段。

## 已归档

- `ddl/sliding_window_retention.sql`
  - 原位置：`scripts/ddl/sliding_window_retention.sql`
  - 归档原因：全仓无引用（`rg -n "sliding_window_retention.sql|scripts/ddl" -S .` 无命中），避免 `scripts/` 噪音。

