交易猫高频系统：构建一套可审计、可回放、可扩展的高频交易数据与执行平台，打通从采集到回测再到实盘的闭环。
 trades 逐笔数据管理模块：统一采集与回填逐笔成交（trades），提供幂等落库、完整性校验、缺口修复与可追溯审计。
 订单薄数据管理模块（首先是；bookDepth，bookTicker）：采集盘口（L1/L2）快照与增量更新，支持重建深度与流动性度量，为执行与风控提供输入。
 metrics 数据管理模块：统一采集交易所发布的权威指标（如 mark/index/funding/openInterest 等），按时间与合约维度落库并可对账回放，为定价、风险与特征工程提供可靠输入。
 派生数据（分时数据）管理模块：将原子数据生成可派生产物（如 K 线/聚合成交/特征切片），并保证每个产物都可由原子数据重建与对账。
 回测模型、自主迭代优化模块：提供可重复回测与训练流水线，支持对照实验、参数搜索与自动评估，推动策略自迭代。
 订单执行管理模块：对接交易所下单与订单生命周期管理，包含限频/风控/日志/回报处理，确保实盘可控可追踪。
 可选，逐笔委托数据
 派生分时ema模型制作，批量跑检查组合，选择采样/聚合频率
 gcp实现tg里面的数据面板在谷歌在线表格中使用
 gcp实现数据流在谷歌在线表格中使用


使用采集，处理，消费分层重构
在线表格终端系统服务构建

