# TradeCat Public References

- `architecture.md`: scope, forbidden paths, module map, and linear data flows.
- `cache-contract.md`: snapshot cache, stream merge, structured latest files, and manifest rules.
- `tui-contract.md`: TUI startup, background probe, rendering, language, and link behavior.
- `install-uninstall.md`: installer, launcher auto-update, and uninstall behavior.

The bundled project lives in `scripts/project/`. Load project source files only when the task requires code-level changes.
